export function getConstructorArgs(_network: string, name: string): unknown[] {
    let args: unknown[] = [];
    switch (name) {
        default: {
            break;
        }
    }
    return args;
}
